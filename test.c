#include <stdio.h>
#include <stdlib.h>

int	main(void)
{
	char	*test;
	// int		i;

	// test = (char *)malloc(sizeof(char) * 2);
	// if (!test)
	// 	return (0);
	// test[0] = 0;
	// i = 0;
	// while (i < 2)
	// 	test[i++] = 't';
	// i = 0;
	// while (i < 10)
	test = NULL;
	printf("%c\n", test[1]);
	return (0);
}
