SIGNAUX:

- Ctrl + C [FINISHED] [TOTEST]
- Ctrl + D [FINISHED] [TOTEST]
- Ctrl + \ [FINISHED] [TOTEST]

UPDATE :
[FINISHED] 'ls | ls | ls' (wrong behaviour)
[FINISHED] 'echo $? + $?'
[FINISHED] 'echo "it works"' (doit afficher comme dans bash)
[FINISHED] 'echo it works' (doit afficher comme dans bash)
[FINISHED]  - 'echo "cat lol.c | cat > lol.c"' (affiche 'cat lol.c | cat > lol.c' sans attendre)
[FINISHED]  - '/bin/ls' (doit s'executer)
[FINISHED]  - '/bin/pwd' (doit pouvoir lancer pwd)
[FINISHED]  - 'cd ~'
[FINISHED]  - 'cd ~/path/of/your/choice/'
[FINISHED]  - 'cd .'
[FINISHED]  - 'cd ../'
[FINISHED]  - 'cd -'
[FINISHED]  - unset
[FINISHED]  - export
[FINISHED]  - status
[FINISHED]  - $HOME
[FINISHED]  - unset $PATH --> ls --> core dumped
[FINISHED]  - Ctrl + D must print exit (hramaros)
[FINISHED]  - exit [TOTEST]
[FINISHED]  - heredoc [EXECUTION && TEST LEFT]
[FINISHED]  - fix le history qui ne marche pas
[FINISHED]  - approche sur le hd fichier temp 'cat << e && cat << e'
[FINISHED]  - '$? || echo $?' (doit faire le passage de status vers la droite)
[FINISHED]  -
            bash-5.1$ /bin/ls 8
            /bin/ls: cannot access '8': No such file or directory
            bash-5.1$ echo $? ==> 2
[FINISHED]  - $ export "LVL "=42 ==> (Minishell: export: `LVL =42': not a valid identifier)
[FINISHED]  - $ export " LVL"=42 ==> (Minishell: export: ` LVL=42': not a valid identifier)
[FINISHED]  - $ export "MSH rl"=42 ==> (Minishell: export: `MSH rl=42': not a valid identifier)
[FINISHED]  - $ export "52"=42 ==> (Minishell: export: `52=42': not a valid identifier)
[FINISHED]  - $ export "52MSH"=42 ==> (Minishell: export: `52MSH=42': not a valid identifier)

[FINISHED]  -
            cat [no-args] ou grep [something] --> echo $? (devient 2)
            Minishell$ cat
            ^\Quit (core dumped)

[FINISHED]  - [hramaros]
            Minishell$ ''
            (doit printer "bash: : command not found")

[FINISHED]  - ?$HOME (ft_search_executable: hramaros)
            - test/minishell (doit pouvoir lancer minishell ou bien No S F o Dir: hramaros)
[FINISHED]
        - 'export' builtin errors handling:
        $ export "LVL" = 42 ==> (Minishell: export: `=': not a valid identifier Minishell: export: `42': not a valid identifier) [manome segfault ny minishell antsika]

    - 'echo -nnnnn' (ne doit rien afficher)
    - 'echo -n -nnn -nnnn' (ne doit rien afficher)
    ...

    ECHO: (lignes 88 `800 tests pour minishell.xlsx`)
    - 'echo $:$= | cat -e' (comportement non conforme a bash, misy leaks)

[FINISHED]
            "$? $?"
            - "$USER"
            export "nom espacer"=value
            make -n
            cat << "e" => shouldn't interpret expanded

TEST SOLVED :
        echo -nnnnn (wrong output)
        echo $:$= | cat -e (wrong output + LEAKS)
        echo " $ " | cat -e (wrong output + LEAKS)
        echo "" $HOME (wrong output)
        echo '' $HOME (wrong output)
        echo "$DONTEXIST" "Makefile" (wrong output)
        echo $=HOME (wrong output)
        "$HOMEdskjhfkdshfsd" (wrong output)
        export = (wrong behaviour)
        export $? (wrong behaviour)
        export ?=2 (wrong behaviour)
        export 9HOLA= (wrong behaviour)
        export HOL@=bonjour (wrong behaviour)
        export HOLA-=bonjour (wrong behaviour)
        export HO-LA=bonjour (wrong behaviour)
        export HOL.A=bonjour (wrong behaviour)
        export HOL}A=bonjour (wrong behaviour)
        export HOL{A=bonjour (wrong behaviour)
        export HO*LA=bonjour (wrong behaviour)
        export HO#LA=bonjour (wrong behaviour)
        export HO@LA=bonjour (wrong behaviour)
        export HO!LA=bonjour (wrong behaviour)
        export HOL+A=bonjour (wrong behaviour)
        export HOLA =bonjour (wrong behaviour)
        export HOLA = bonjour (wrong behaviour)
        export TE+S=T (wrong output)
        export ''='' (wrong behaviour)
        export "="="=" (wrong behaviour)
        export '='='=' (wrong behaviour)
        unset 9HOLA (wrong output)
        unset $PWD (wrong behaviour)
        unset ++++++ (wrong behaviour)
        pwd -p (wrong output)
        unset HOME && cd $HOME (wrong output)
        env -i ./minishell (wrong behaviour)
        echo $HOLA | cat -e (LEAKS)
        << hola (wrong status return)
        ls | exit 42 (wrong behaviour for status return)
        cat < fichierinexistant (wrong behaviour)
        ls | eof (wrong status return)
        cat and ls with folder as input file
        cd // && pwd (wrong output)
        << $"hola"$"b" (wrong EOF parsed)
        << ho$la$"$a"$$"b" (wrong EOF parsed)
        
        - '> foo' (wrong behaviour)
        - > file.txt (wrong behaviour + -rw-r--r--) (ballain)
        - export "" (wrong behaviour )
        
        - export -HOLA=bonjour (wrong behaviour ) <=== not wrong, shouldn't to check
        - export --HOLA=bonjour (wrong behaviour ) <=== not wrong, shouldn't to check
        - export "MSH52"=42 (wrong behaviour)
        - export "test=test"=TEST (wrong behaviour)
        - time sleep 3 | sleep 3 (invalid read of size 4)
        - 'cat | cat | ls' (invalid read of size 4)
        - 'cat /dev/random | ls' (invalid read of size 4)
        - cat | ls + Ctrl^C (invalid read of size 4 + LEAKS)
        - 'mkdir a && mkdir a/b && cd a/b && rm -r ../../a && pwd' ( LEAKS quand on fait 'exit' apres la commande) <==== leaks apres exit !!!!!!!
    
        << $"hola"$"b"$'' (wrong EOF)
        << $"hola"$"b"$"" (wrong EOF)


        /bin/echo hi >./outfiles/outfile01 | /bin/echo bye
        ls >./outfiles/outfile01
        <tcr.sh grep -v $ | wc -l >> $abc123 (status error)
        /bin/echo <123 <456 hi | /bin/echo 42
        <$var123 cat
        <$var123 <$var123 cat
        Minishell$ unset PWD OLDPWD (then) -----> Minishell$ cd / (segfault) 
        $NOVAR
        "."

TESTS UNSOLVED :
    export T="l"                <======== (MOI PAS COMPRENDRE CELUI LA)
    echo "<<| echo wtf"         <======== (Your checker)

TODO BEFORE VOGSPHERERE:
    - supprimer: sujet.pdf, TODO, README.md
    - verifier tous les header de fichiers

URGENT :
    bash-5.1$ valgrind --leak-check=full --track-origins=yes --show-leak-kinds=all --suppressions=supp.supp -s ./minishell